/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
        ignoreDuringBuilds: true,
    },
    // reactStrictMode: true,
    // images: {
    //     domains: ['https://arun-omnify-task.vercel.app/'], // Replace with your allowed domains
    // },
};

export default nextConfig;
